# ===========================================
# REQUIRED: JWT Authentication Configuration
# ===========================================
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=change-this-to-a-secure-random-key-at-least-32-chars

# ===========================================
# REQUIRED: Database Configuration
# ===========================================
# For HuggingFace Spaces, use SQLite (default)
DATABASE_URL=sqlite:///./database/auth.db

# For PostgreSQL (optional, requires external database):
# DATABASE_URL=postgresql+asyncpg://user:password@host:port/database

# ===========================================
# REQUIRED: CORS Configuration
# ===========================================
# Comma-separated list of allowed origins for frontend
ALLOWED_ORIGINS=https://mrowaisabdullah.github.io,https://huggingface.co

# ===========================================
# OPTIONAL: OAuth Configuration (Google)
# ===========================================
# Only needed if you want Google OAuth login
# Get credentials from: https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret
# AUTH_REDIRECT_URI=https://your-space.hf.space/auth/google/callback

# ===========================================
# OPTIONAL: API Configuration (has defaults)
# ===========================================
# API_HOST=0.0.0.0
# API_PORT=7860
# LOG_LEVEL=INFO

# ===========================================
# OPTIONAL: JWT Settings (has defaults)
# ===========================================
# JWT_ALGORITHM=HS256
# JWT_EXPIRE_MINUTES=10080